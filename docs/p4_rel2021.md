# P4 REL2021 grid — openness verdict + demo/coverage note (issues #274, #348)

> Verdict date: 2026-09-13 (7 single polite GETs, 3 s pacing, contact UA
> `home-finder-research/0.1 (... issues 274/348)` in headers, cached
> `/tmp/hf-p4-rel2021/`).
> Code: `services/scoring/dims_p4_rel2021.py` (demo P4-025 + 7 coverage
> params); tests: `services/scoring/tests/test_dims_p4_rel2021.py`
> (hermetic, synthetic fixtures, no network).

## Verdict

| Source | Result | Evidence (2026-09-13) |
|---|---|---|
| Statistikaamet **PX-Web API root** (`/api/v1/et`) | **OPEN** | HTTP 200, 67 B JSON: `[{"dbid":"stat",...},{"dbid":"statsql",...}]` |
| PX-Web **stat DB** (`/api/v1/et/stat`) | **OPEN** | HTTP 200, 7 folders incl. `rahvaloendus` (Rahva ja eluruumide loendus) |
| PX-Web **rahvaloendus** folder | **OPEN** | HTTP 200, 4 entries incl. `rel2021` (Rahva ja eluruumide loendus 2021) |
| PX-Web **rel2021** folder | **OPEN** | HTTP 200, 9 topic folders (elamistingimused, eluruumid, haridus, leibkonnad, põlisus-ja-ränne, demograafilised, majanduslik-aktiivsus, paiknemine, tervis) |
| PX-Web **rahvastiku_paiknemine** folder | **OPEN** | HTTP 200, 2 folders (`elukoht-ja-soo-vanusjaotus`, `teine-elukoht`) |
| REL2021 **age tables** (`.../elukoht-ja-soo-vanusjaotus`) | **OPEN (asula-level)** | HTTP 200, 7 tables (RL21001/RL21003/RL21004/RL21005/RL21011/RL21037/RL21040); titles read `ELUKOHA (HALDUSÜKSUS)` / `(ASULA)` / `TIHEASUSTUSEGA PAIKKONNA` — no grid level |
| Table metadata **RL21003** (population by residence/sex/age) | **OPEN, finest = asula** | HTTP 200, 205 392 B JSON; `Elukoht` variable has 4868 values (settlements) — confirms no 1 km-grid dimension in the open tree |
| REL2021 **1 km-grid bulk** | **DATED NEGATIVE — no open bulk** | No grid-level table or download endpoint in the traversed open tree; therefore no grid-bulk URL is stored in `SOURCE_URLS` (nothing unverified is ever fetched) |

Consequence (honest shape): the exact cell-ID join is implemented and
fixture-proven, but with no open 1 km-grid bulk, **production grid joins
stay NULL** with an Estonian reason pointing at the missing open grid bulk
until one appears. The openly ingestible asula-level tables back the
labelled P4-025 asula fallback. Re-probe annually (`TTL_DAYS rel_tables:
365`); a newly opened bulk flips the verdict without scorer changes
(ingestion already caches with TTL; `grid_bulk: 91` standby).

Note: `dims_group20a.py` (#212) mentions "REL2021 1 km grid tables
RL21004/RL21202" — RL21004 in the open tree is an **asula-level** table
(eestlaste arv ja osatähtsus elukoha (asula) järgi), not a grid table.
That docstring's grid claim should be read as the missing source, not an
available one; this verdict is the dated evidence.

## Pull policy (polite, cached, TTL-stated)

- One request per source per run; `User-Agent: home-finder-research/0.1`
  (polite annual bulk; issues 274/348); 25 s timeout; 3 s pacing between
  requests (7 requests total for this verdict, smallest responses first).
- `fetch_cached(url, cache_dir, name, ttl_days)`: fresh cache wins (no
  request); transport errors are raised and **never cached as data**; HTTP 429
  raises immediately (stop signal, no retry).
- TTLs: REL tables **365 d** (frozen 31.12.2021 census + annual grid-bulk
  re-probe), grid bulk **91 d** standby (quarterly once a bulk appears).

## Per-param wiring (all honest shapes = exact cell join or asula choropleth)

| Param | Wired leg (this ingestion) | Legs honestly missing (EI OLE, named not faked) |
|---|---|---|
| P4-025 micro-liquidity (demo) | cell vacancy + net migration + age balance; asula youth-share fallback | school open/close plans; tehingud turnover (sibling module) |
| P4-003 rent reality | cell rental-share + vacancy (hex nuisance hinnang) | Airbnb density, KV medians |
| P4-011 kindergarten/GP | cell 0–6 cohort (demand-pressure leg) | Haridusamet queue table, GP lists |
| P4-043 number-13 | 13-mark × street residual, cell education share as prestige control | price-history leg (point residual only) |
| P4-044 herd of picky people | cell creative-occupation share (taste-match, never worth judgement) | tehingud price drift (sibling module) |
| P4-045 third places | cell evening/day population ratio (demand leg) | OSM evening hours, keeper independents |
| P4-051 zero-consumption | cell vacancy share (cell-level only, never addresses) | Elektrilevi/Vesi aggregates, KÜ funds |
| P4-052 turnover wave | cell churn ≥ 150/1000 → neutral 50, both readings | tehingud per-building wave (sibling module) |

Never a gradient: no distance weighting, no neighbour smoothing, no
interpolation, exact-ID joins only; thin/suppressed cells (< 100 residents)
never score (MIN_CELL_POP = 100); P4-052 ambiguity scores neutral 50 so it
cannot push the steal sort.
